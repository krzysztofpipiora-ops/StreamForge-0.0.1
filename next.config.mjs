/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Jeśli masz tutaj jakieś inne specyficzne ustawienia (np. images), możesz je tu zostawić
};

export default nextConfig;
